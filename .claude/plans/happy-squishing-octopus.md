# Plan: Major Project Cleanup

## Context

After E2E testing of the Stickerlandia onboarding flow, the repo has accumulated test artifacts, orphaned files, old transcript/reference docs, and disorganized scripts. The goal is to leave only clean, documentable skill/agent definitions, example repos, and useful tooling — plus spin the action catalog extractor into its own skill.

## Part 1: Delete files and directories

### Root-level files to delete
- `advocacy-kickoff-transcript.txt` — old meeting transcript, not project documentation
- `skils-guide.md` — Anthropic's public skills guide, not project-specific

### Generated test output to delete
- `datadog-resources/` — entire directory (8 generated Python scripts from Stickerlandia onboarding run)
- `test-projects/stickerlandia/datadog-recommendations.md` — generated by repo-analyzer
- `test-projects/stickerlandia/repo-analysis.json` — generated by repo-analyzer
- `.claude/context/repo-analysis.json` — transient context file from last run

### Test harness to delete
- `scripts/testing/` — entire directory (setup_wizard.py, dd_cleanup.py, dd_verify.py, lib/, all `__pycache__/`)
- `scripts/cleanup_stickerlandia.py` — one-shot cleanup script, already run, purpose served

### Skill test artifacts to delete
- `.claude/skills/app-builder/examples/python/test-scripts/` — entire directory (8 test_*.py files)
- `.claude/skills/dashboards/examples/python/__pycache__/` — bytecode cache

### Workflow-automation orphaned files to delete
These are old AWS GameDay quest extracts, not useful examples:
- `.claude/skills/workflow-automation/examples/python/create_workflows.py`
- `.claude/skills/workflow-automation/examples/python/setup_aws_action_connection.py`
- `.claude/skills/workflow-automation/examples/python/test_workflow_action_ids.py`
- `.claude/skills/workflow-automation/examples/python/action_connection_setup.py`
- `.claude/skills/workflow-automation/examples/AWS_ACTION_CONNECTION_SETUP.md` — orphaned doc referencing old quest paths
- `.claude/skills/workflow-automation/examples/datadog_action_role_cfn_snippet.yaml` — CFN snippet better covered by action-connections skill

### Workflow-automation orphaned JSON to relocate
These 3 JSON files are sitting at `examples/` root instead of `examples/json/`:
- `examples/AWS-IAM-Disable-User.json` → move to `examples/json/`
- `examples/ECS-Rollback-Leaderboard.json` → move to `examples/json/`
- `examples/AWS-EC2-Require-IMDS-v2_spec.json` → move to `examples/json/`
- `examples/AWS-IAM-Revoke-Permissions.json` → move to `examples/json/`

### macOS junk
- `.claude/.DS_Store`

## Part 2: Update .gitignore

Add entries to prevent future accumulation:
```
# Generated outputs
datadog-resources/
**/repo-analysis.json
**/datadog-recommendations.md

# macOS
.DS_Store
**/.DS_Store
```

## Part 3: Create `action-catalog-extractor` skill

Spin `scripts/extract_action_catalog.py` into a proper skill so it's documented and discoverable.

### New files
- `.claude/skills/action-catalog-extractor/SKILL.md` — skill definition following the standard pattern
- `.claude/skills/action-catalog-extractor/examples/python/extract_action_catalog.py` — move existing script here

### Delete after move
- `scripts/extract_action_catalog.py` — moved into skill

### SKILL.md content outline
- **Name**: action-catalog-extractor
- **When to use**: Refreshing the shared action catalog from dd-source
- **Prerequisites**: Access to `dd-source` repo, Python 3
- **Core workflow**: Run the script with `--bundles-dir`, `--output-dir`, `--index-file`, `--prefixes`, `--stability` flags
- **Output**: Per-service .md files in `.claude/skills/shared/actions-by-service/` + master index
- **Cross-skill notes**: Output is consumed by workflow-automation skill

### New agent (optional, lightweight)
- `.claude/agents/action-catalog-extractor.md` — simple agent that loads the skill

## Part 4: Clean up empty directories

After all deletions, remove any directories that become empty:
- `scripts/testing/`
- `.claude/skills/workflow-automation/examples/python/` (if empty after deletions — check if anything else lives there)
- `datadog-resources/`

If `scripts/` directory becomes empty after moving extract_action_catalog.py, remove it too.

## Summary of changes

| Action | Count |
|--------|-------|
| Files/dirs deleted | ~25 files + 3 directories |
| Files moved | 5 (4 JSON relocations + 1 script → skill) |
| Files created | 2 (SKILL.md + agent .md for action-catalog-extractor) |
| Files modified | 1 (.gitignore) |

## Verification

1. `git status` — confirm only intended changes
2. `find . -name '__pycache__' -not -path './.git/*'` — no stray bytecode
3. `find . -name '.DS_Store' -not -path './.git/*'` — no macOS junk
4. Confirm skill directories still follow the pattern: `SKILL.md`, `references/`, `examples/{python,terraform,json}/`
5. Verify the action-catalog-extractor script still runs: `python3 .claude/skills/action-catalog-extractor/examples/python/extract_action_catalog.py --help`
6. Spot-check that no skill SKILL.md references deleted files
